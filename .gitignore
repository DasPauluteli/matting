/target
**/target

# ONNX weights and MIGraphX compiled caches are large and regenerable via
# `matting prepare`.
/models/
*.mxr
/.mxr-cache/
/models_unpatched/

# Throwaway venv from the spike phase.
/.venv-tools/
# Design and planning notes stay local.
/docs/
